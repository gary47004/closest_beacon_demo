# Uncomment this line to define a global platform for your project
platform :ios, ‘8.0’

target 'closest_beacon_demo' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for closest_beacon_demo
	pod ‘Firebase’
	pod ‘Firebase/Database’


  target 'closest_beacon_demoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'closest_beacon_demoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
